server:
  port: 8081
spring:
  application:
    name: SCHOOL-SERVICE
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    hostname: localhost

  