server:
  port: 8082
spring:
  application:
    name: STUDENT-SERVICE
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
    hostname: localhost
  